apiVersion: batch/v1

kind: Job

metadata:
  name: playwright-tests

spec:

  template:

    spec:

      containers:

        - name: playwright

          image: docker4deepak/playwright-framework:v1

          imagePullPolicy: Always

      restartPolicy: Never

  backoffLimit: 1